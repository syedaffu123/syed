# include<stdio.h>
void main()
{
	int it;
	char ct;
	double dt;
	float ft;
	printf("the size of integer is :%ld\n",sizeof(it));
	printf("the size of float is   : %ld\n", sizeof(ft));
	printf("the size of char is    : %ld\n" , sizeof(ct));
	printf("the size of doub is    : %ld\n" , sizeof(dt));
} 
